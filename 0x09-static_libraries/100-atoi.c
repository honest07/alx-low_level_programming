#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _atoi - Entry point that converts a string into
 * an integer
 * @s: the string to use.
 * Return: integer
 */
int _atoi(char *s)
{
	return (atoi(s));
}
